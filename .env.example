# Environment Variables for AI Chat Function

# ============================================================================
# CURRENT IMPLEMENTATION (Google Gemini)
# ============================================================================

# Required: Google Gemini API Key
# Get your API key from: https://makersuite.google.com/app/apikey
# Replace the placeholder below with your actual API key
GEMINI_API_KEY=

# Optional: Redis URL for production rate limiting and caching
# Format: redis://[username:password@]host:port
# If not set, the function will use in-memory storage (suitable for development)
# REDIS_URL=redis://localhost:6379

# Optional: API Authentication Token
# If set, clients must include this token in the Authorization header
# Format: Authorization: Bearer YOUR_TOKEN
# If not set, authentication is disabled (backwards compatible)
# API_AUTH_TOKEN=

# ============================================================================
# RECOMMENDED UPGRADE: VERCEL AI SDK (Multi-Provider Support)
# ============================================================================
# Uncomment and configure these variables to enable the upgraded AI integration
# with streaming support and multi-provider capabilities.
# See AI_INTEGRATION_RECOMMENDATION.md for details.

# OpenAI API Key (recommended for fallback and advanced features)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=

# Anthropic API Key (optional, for Claude models)
# Get your API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=

# Default AI Provider (choose one: gemini, openai, anthropic)
# The system will automatically fallback to other providers if the default fails
# DEFAULT_AI_PROVIDER=gemini
